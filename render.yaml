services:
  - type: web
    name: ai-chat
    env: static
    buildCommand: ./render-build.sh
    staticPublishPath: ./
    envVars:
      - key: GITHUB_TOKEN
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
